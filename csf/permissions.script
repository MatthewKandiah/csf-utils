#!/bin/bash --login


echo "Update permissions in this directory and all subdirectories so .script file is executable"
for script in $(find . "*.script")
do
	echo "Updating permissions for $script"
	chmod 744 $script
done
echo "permissions.script done!"
